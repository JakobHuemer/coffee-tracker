{
  "name": "coffee-tracker",
  "private": true,
  "description": "Root task runner. Holds no dependencies — server/ and client/ install independently, exactly as CI does. Scripts mirror .github/workflows/pr-checks.yaml so a green `bun run check` locally means a green PR.",
  "scripts": {
    "test": "cd server && bun test",
    "test:watch": "cd server && bun test --watch",
    "lint": "cd client && bun run lint",
    "build": "cd client && bun run build",
    "check": "bun run test && bun run lint && bun run build"
  }
}
